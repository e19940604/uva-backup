#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <stack>
using namespace std;

int main(){

int grade;
for( grade = 1 ; grade < 4 ; grade++ );
if(grade == 4 )
    grade++;

printf("你將會讀%d年\n",grade);

}
